# SEQUENCE-DETECTOR
A sequence detector is a digital circuit used to detect a specific pattern or sequence of bits in a binary input stream. It produces an output signal when the desired sequence is detected. It is commonly designed using finite state machines (FSMs) and can be implemented as **Mealy or Moore machines**.
